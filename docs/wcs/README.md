# 台州海柔飞箱项目 — WCS 子 PRD 交付物

主 PRD：《【项目】【P0】台州海柔飞箱项目》v1.1
本目录为其中 **WCS 模块**的子 PRD（当前 **v1.1**），用于 2026-08-12 与研发、测试同学评审。

## 交付物清单

| 文件 | 格式 | 用途 |
|---|---|---|
| `WCS子PRD_台州海柔飞箱项目.md` | Markdown | 主版本。图示用 mermaid，GitHub / IDE 直接渲染，便于版本管理与 diff |
| `WCS子PRD_台州海柔飞箱项目.docx` | Word | 评审分发版。含封面、目录、页眉页脚、6 张图，共 39 页 |
| `WCS子PRD_台州海柔飞箱项目_飞书版.md` | Markdown | 飞书导入版。每张图同时给出图片引用与等价表格 |
| `images/*.png` | PNG | 由 `diagrams/*.mmd` 渲染，供 Word 版与飞书手动插图使用 |
| `diagrams/*.mmd` | Mermaid | 图示源文件，改图后重新渲染即可 |

三份文档的**正文内容完全一致**，只有图示的承载方式不同。

## 导入飞书的两种方式

**方式一（推荐，保留图片与排版）**
飞书云文档 → 新建 →「导入文档」→ 上传 `WCS子PRD_台州海柔飞箱项目.docx`。
6 张图、表格、目录会一并带入。

**方式二（纯文本，图示另有等价表格兜底）**
打开 `WCS子PRD_台州海柔飞箱项目_飞书版.md`，全选内容粘贴进飞书文档。
飞书会自动识别标题层级与表格；图片链接不会自动解析，需从 `images/` 手动插入到对应位置，
但每张图下方都附了等价表格，不插图也不影响评审阅读。

> 本次生成环境未接入飞书连接器，无法直接在飞书空间内创建文档，因此以可导入文件的形式交付。

## 重新生成图片

```bash
npx -y @mermaid-js/mermaid-cli@11 -i diagrams/04-state.mmd -o images/04-state.png -b white -s 2
```

`.md` 正文里每张图前都有 `<!-- wcs-img: NAME.png -->` 标记，Word 版与飞书版据此定位图示，
新增或调整图示顺序不会串位。

## 图示与章节对应关系

| 图片 | 对应章节 |
|---|---|
| `01-arch.png` | §2.2 WCS 系统定位 |
| `02-seq-replenish.png` | §3.2 主流程时序（补货上架） |
| `03-seq-pick.png` | §3.3 主流程时序（拣货出库） |
| `06-er.png` | §5.1 领域模型实体关系 |
| `04-state.png` | §5.2.2 状态流转图 |
| `05-cancel.png` | §6.4 补货任务取消流程 |

## 评审前需要先看的三处

- **§4 标准指令模型（已定稿）** — 模型不再比选。重点看 **§4.5 三厂商建模推演**（海柔闪拣 / 立镖飞箱 / 原力聚合四向车），结论是三个场景表结构零变更；推演同时暴露并修正了两处字段设计（载具泛化 `carrier_code`+`carrier_type`、新增 `uom` 单位），这两列**必须在 1 期就位**。
- **§9 运维后台 + 人工兜底 SOP** — 1 期不做系统级超时告警，改人工兜底，后台的查询与人工重推是 1 期 P0。
- **§12 问题清单** — §12.1 是已确认结论（6 条），§12.2 是仍待确认（15 条）。

## 当前唯一的阻塞项

**§12.2 Q2：《台州飞箱（WCS-海柔接口）》尚未拿到**（附件未成功送达）。
海柔侧字段与接口粒度无法对齐，§7.6 映射表仍是占位。拿到后 3 个工作日内可补全。
WMS ↔ WCS 的标准契约（I-01～I-12）不受影响，可先行评审与开发。

## 版本记录

| 版本 | 日期 | 主要变更 |
|---|---|---|
| v1.0 | 2026-08-08 | 首次创建，基于主 PRD v1.1 + 两轮需求澄清 |
| v1.1 | 2026-08-08 | 指令模型定稿并新增三厂商建模推演；快照时间统一 07:00；重试口径定为首次+3 次共 4 次；运维后台纳入 1 期 P0、超时改人工兜底；WMS 按 WCS 标准契约实现回传接口；载具/单位字段泛化；新增 ER 图 |
